<view class="menu">
  <view class="menu-item">
    <block wx:for="{{imageList}}" wx:key="{{index}}" >
      <view bindtap='toWeb' data-item="{{item}}">
        <image src="{{item.url}}"></image>
        <view class='menu-name'>{{item.name}}</view>
      </view>
    </block>
  </view>

</view>